[package]
name = "lettre"
version = "0.1.0"
edition = "2024"

[dependencies]
# 开启 tokio 支持
lettre = { version = "0.11", features = ["tokio1-native-tls", "hostname"] }
# 引入异步运行时
tokio = { version = "1", features = ["full"] }
dotenvy = "0.15"